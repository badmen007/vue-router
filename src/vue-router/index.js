import install,{Vue} from './install';

class VueRouter {
    constructor(options) {

    }

};

//1.需要将用户的配置进行映射
//2.要将根实例注入的router属性共享到所有的组件

//这个方法是干啥的？

VueRouter.install = install;

export default VueRouter;